# Frontend environment (copy to .env.local if you need to override).
# Leave empty in development: the Vite dev server proxies /api to the backend.
# For a deployed backend, set the absolute origin, e.g.:
# VITE_API_BASE=http://192.168.1.10:8000
VITE_API_BASE=
